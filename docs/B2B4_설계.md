우리가 해야하는 일: 
- B4: page fault가 진짜로 잘못된 접근인지 아니면 spt에서 꺼내와야 하는 건지 검사
- B2: lazy load: page fault가 들어오면 spt에 빈 껍데기만 들어있던 page를 진짜로 frame이랑 매핑

### B2
``` c
/* palloc()을 호출해 프레임을 가져옵니다. 사용 가능한 페이지가 없으면 페이지를
 * 축출하고 반환합니다. 이 함수는 항상 유효한 주소를 반환합니다. 즉, 사용자 풀
 * 메모리가 가득 차면 사용 가능한 메모리 공간을 얻기 위해 프레임을 축출합니다. */
static struct frame *
vm_get_frame (void) {
	struct frame *frame = NULL;
	/* TODO: 이 함수를 채웁니다. */
- pml4_set_page

- 예외 처리 추가
	ASSERT (frame != NULL);
	ASSERT (frame->page == NULL);
	return frame;
}
```

``` c
frame_table_insert () {
- list_push_back(프레임)
}
```
``` c
/* PAGE를 claim하고 MMU를 설정합니다. */
static bool
vm_do_claim_page (struct page *page) {
	struct frame *frame = vm_get_frame ();

	/* 링크를 설정합니다. */
	frame->page = page;
	page->frame = frame;

	/* TODO: 페이지의 VA를 프레임의 PA에 매핑하는 페이지 테이블 엔트리를 삽입합니다. */
- frame_table_insert(프레임)
- spt_page_insert(page)
- mmu 설정

	return swap_in (page, frame->kva);
}
```
``` c
vm_claim_page(){
	- va를 pg_round_down으로 page boundary에 맞춘다
    - 현재 thread의 spt에서 spt_find_page()로 page를 찾는다
    - 없으면 false 반환
    - 있으면 vm_do_claim_page(page) 호출
}
```
``` c
pml4_set_page(): 논리주소를 물리주소랑 매핑?
- 이걸 어디다가 쓸 지만 결정하면 될듯
```

### B4

page fault가 났을 때 spt에서 꺼내와야?꺼내올수 있는지

0. addr이 NULL이 아니면 아래로 내려감
1. not_present가 true 이면 아래로 내려감
2. user 주소에 해당하는 페이지여야 아래로 내려감
3. spt에 이 주소에 해당하는 페이지가 있어야 아래로 내려감
4. writeable이 true인데 쓰기로 접근한 경우에만 아래로 내려감
- 하나라도 해당이 안 되면 goto exit(-1)