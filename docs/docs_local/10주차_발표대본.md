**안녕하세요. 이경근입니다. 저는 file syscall에 대해 말해보고자 합니다.**

**이 작업을 통해 가장 크게 배운 건, 돌아가는 코드와 안정적인 코드는 다르다는 점이었습니다.**

**처음에는 open, read, write, close를 각각 따로 구현하면 된다고 생각했습니다. 테스트만 보면 필요한 함수만 연결해도 충분해 보였습니다.**

**하지만 구현을 하다 보니 핵심은 syscall 하나가 잘 도는지가 아니라, 파일 자원이 언제 생기고 누가 들고 있다가 언제 정리되는지였습니다. 그래서 프로세스별 fd_table로 fd와 열린 파일을 연결하고, 종료 시 남은 fd까지 정리하도록 했습니다.**

**이 과정에서 실수도 있었습니다. fd table을 정리할 때 원본 list가 아니라 복사본을 순회하려고 했고, file_close에 넘겨야 하는 포인터의 레벨을 잘못 생각하기도 했습니다. 이런 실수는 단순 문법 문제가 아니라, 제가 지금 무엇을 소유하고 있고 무엇을 가리키고 있는지 정확히 이해하지 못했기 때문에 생긴 문제였습니다.**

**그리고 나중에 dup2를 보며 fd와 file의 1대1 연결만으로는 부족하고, 공유되는 파일 상태와 close 시점까지 고려해야 한다는 걸 알게 됐습니다.**

**이번 작업은 syscall 구현보다, 기능이 시스템의 자원 흐름 속에서 어떤 역할을 하는지를 배운 과정이었습니다. 앞으로는 어떤 기능이든 동작 여부뿐 아니라 전체 시스템에 미치는 영향까지 함께 보려고 합니다.**